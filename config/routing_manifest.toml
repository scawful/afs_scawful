# Routing manifest: which lane serves which weights, on which box.
#
# A lane is a name clients request. `serves` names the exact weights allowed to answer to it, by
# sha256, because a box relabels models (LM Studio renames `m` to `m@quant` once a second quant of
# it exists) and a name alone does not pin what is behind it.
#
# Requesting a lane whose weights are not present is an error. No lane answers with other weights.
# Promotion is editing `serves` here; it is not a code change and not a deploy.
#
# sha256 is the full 64-hex digest of the file, compared for exact equality. A prefix would let a
# different file with a shared prefix satisfy a lane, and prefixes cannot be lengthened later
# without a flag day, so the manifest never carries one.

version = 1

# -- Scawfulbot lanes (medical-mechanica, RTX 5090, LM Studio on :1234) ------------------------

[[lanes]]
id = "scawfulbot-qwen35"
display_name = "Scawfulbot"
description = "Justin's voice. Promoted 2026-09-16 after the Claude judge eval."
also_answers_to = ["scawfulbot"]
on_unavailable = "error"

  [[lanes.serves]]
  host = "medical-mechanica"
  provider = "lmstudio_win"
  model = "qwen35-curated-masked"
  quant = "q8_0"
  sha256 = "b4490ba25882fe825ebd95c4a4af50d419b59e5a6204807f9dc24fa8438fea48"

  [lanes.provenance]
  # Every number here comes from eval/run_evals.jsonl in the scawfulbot repo.
  corpus_sha256 = "c20695b8c9c2"
  corpus_rows = 19047
  recipe = "qwen3.5-9b QLoRA, lr 5e-5, 1 epoch, train-on-responses-only"
  eval_pass_system = 0.47
  eval_pass_no_system = 0.32
  judged = "2026-09-16"
  promoted = "2026-09-16"

[[lanes]]
id = "scawfulbot-voicefix"
display_name = "Scawfulbot (voice-fix SFT)"
description = "First corpus-fix run. Kept addressable to compare against the promoted lane."
on_unavailable = "error"

  [[lanes.serves]]
  host = "medical-mechanica"
  provider = "lmstudio_win"
  model = "qwen35-voicefix-small"
  quant = "q8_0"
  sha256 = "ead32f5bc5016f4bffd4d9755f6a2cacb836f9efe988e73c7c17521fde387e49"

  [lanes.provenance]
  corpus_sha256 = "a559ea39b338"
  corpus_rows = 8174
  recipe = "qwen3.5-9b QLoRA, lr 5e-5, 1 epoch, loss on all tokens"
  eval_pass_system = 0.31
  eval_pass_no_system = 0.22
  judged = "2026-09-16"

[[lanes]]
id = "scawfulbot-qwen35-v1-dpo-q8"
display_name = "Scawfulbot (April DPO)"
description = "Superseded 2026-09-16. Kept addressable as the eval baseline."
also_answers_to = ["scawfulbot-april"]
on_unavailable = "error"

  [[lanes.serves]]
  host = "medical-mechanica"
  provider = "lmstudio_win"
  model = "qwen35-v1-dpo"
  quant = "q8_0"
  sha256 = "023b1db708eb417bb9a6fa31c7a6bff3bb60594cc966f0ea93b0e987194b770f"

  [lanes.provenance]
  eval_pass_system = 0.03
  eval_pass_no_system = 0.0
  judged = "2026-09-16"

# -- Oracle lanes (Zelda / ROM-hacking models, same box) ---------------------------------------

[[lanes]]
id = "oracle-9b"
display_name = "Oracle 9B"
description = "Oracle of Secrets assistant, no-think variant."
also_answers_to = ["oracle-9b-v5"]
on_unavailable = "error"

  [[lanes.serves]]
  host = "medical-mechanica"
  provider = "lmstudio_win"
  model = "gguf/zelda/oracle-9b-candidate-v5-nothink-q4km.gguf"
  sha256 = "e592b09f181a41c42496d5abe0d4a83b3efaddd59f6be82b6fffa508666aa5a5"

[[lanes]]
id = "oracle-14b"
display_name = "Oracle 14B"
description = "Larger Oracle model for escalation."
also_answers_to = ["oracle-14b-v8"]
on_unavailable = "error"

  [[lanes.serves]]
  host = "medical-mechanica"
  provider = "lmstudio_win"
  model = "gguf/zelda/qwen3-oracle-14b-v8-q4km.gguf"
  sha256 = "b2e026193c0bcc58f9f9dd01568528080d5d58425dc055d86c736c13c0fe333b"

# -- Cloud models are deliberately NOT lanes ---------------------------------------------------
# A lane pins weights by sha256, which only means something for models we train and host. Cloud
# models keep their static specs, whose identity is the provider's and which carry provider-specific
# settings (api mode, dated ids) a lane does not express. The app chooses them explicitly; nothing
# here ever substitutes one for a lane above.

# -- Not lanes yet -----------------------------------------------------------------------------
# The Oracle personas (nayru, din, farore, veran, majora) are advertised by the gateway's static
# specs but medical-mechanica does not index those GGUFs, so nothing can serve them. They are left
# out deliberately rather than declared and dead. Adding one means importing it into LM Studio,
# recording its sha256, and adding a lane here.
#
# Assistant lanes (tasks, home) will declare `tools = [...]` alongside `serves`, so a lane can be a
# model plus the tools it is allowed to call.
